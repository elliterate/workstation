require 'rubygems'
require 'irb/completion'
require 'pp'

begin
  require 'irb/ext/save-history'
rescue LoadError
  # History-saving became a core feature in v1.7.2
end

begin
  require 'ap'
rescue LoadError
end

begin
  require 'wirble'
rescue LoadError
  IRB.conf[:PROMPT_MODE] = :SIMPLE
else
  Wirble.init
  Wirble.colorize
end

begin
  require 'interactive_editor'
rescue LoadError
end

IRB.conf[:SAVE_HISTORY] = 1000
IRB.conf[:HISTORY_FILE] = "#{ENV['HOME']}/.irb_history"
