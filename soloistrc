recipes:

# base
- base
- base::bash_it
- homebrew
- homebrew::install_formulas

node_attributes:
  homebrew:
    formulas:
      - ack
      - ag
      - pstree
      - tree
      - watch
      - wget
